name: Build Lofter Fixer (Surgical Fix)
on: workflow_dispatch

env:
  APP_NAME: "LOFTER去水印"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'

      # 1. 备份你的核心代码和配置
      - name: Backup Local Files
        run: |
          mkdir -p user_backup
          
          # 备份 Kotlin 核心逻辑
          if [ -f "android/app/src/main/kotlin/com/example/lofter_fixer/MainActivity.kt" ]; then
             cp android/app/src/main/kotlin/com/example/lofter_fixer/MainActivity.kt user_backup/
             echo "✅ Backed up MainActivity.kt"
          fi
          
          # 备份 Manifest (保留你的权限配置)
          if [ -f "android/app/src/main/AndroidManifest.xml" ]; then
             cp android/app/src/main/AndroidManifest.xml user_backup/
             echo "✅ Backed up AndroidManifest.xml"
          fi

      # 2. 重建标准工程 (关键！这会生成 gal 插件需要的标准环境)
      - name: Recreate Standard Android Project
        run: |
          echo "🧨 Cleaning old android directory..."
          rm -rf android
          
          echo "🔨 Generating standard Flutter Android scaffold..."
          # 让 Flutter 自己生成标准文件，包含了插件需要的 hook
          flutter create . --platforms android --project-name lofter_fixer

      # 3. 恢复你的代码
      - name: Restore User Code
        run: |
          # 恢复 Kotlin
          TARGET_DIR="android/app/src/main/kotlin/com/example/lofter_fixer"
          mkdir -p $TARGET_DIR
          cp user_backup/MainActivity.kt $TARGET_DIR/
          
          # 恢复 Manifest
          if [ -f "user_backup/AndroidManifest.xml" ]; then
             cp user_backup/AndroidManifest.xml android/app/src/main/AndroidManifest.xml
          fi
          echo "✅ Code restored."

      # 4. 💉【外科手术修改】修改 Gradle 配置 (不覆盖文件，只改内容)
      - name: Modify Gradle Configs (Surgical)
        run: |
          # A. 修改 settings.gradle 强制指定 Kotlin 1.9.0
          # 这一步是为了解决新版插件的冲突
          sed -i 's/id "org.jetbrains.kotlin.android" version ".*"/id "org.jetbrains.kotlin.android" version "1.9.0"/g' android/settings.gradle
          
          # B. 修改 app/build.gradle 调整 MinSDK 为 24 (OpenCV/Gal 需要)
          # 我们查找 "flutter.minSdkVersion" 并将其替换为 "24"
          sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 24/g' android/app/build.gradle
          # 如果上面没找到（不同版本差异），尝试替换默认写法
          sed -i 's/minSdkVersion.*/minSdkVersion 24/g' android/app/build.gradle

          # C. 注入原生依赖 (OpenCV, TFLite)
          # 我们在 "dependencies {" 这一行后面插入我们的库
          # 这是一个精准的插入操作，不会破坏文件结构
          sed -i '/dependencies {/a \
              implementation "org.jetbrains.kotlin:kotlin-stdlib:1.9.0"\
              implementation "org.tensorflow:tensorflow-lite:2.14.0"\
              implementation "org.tensorflow:tensorflow-lite-support:0.4.4"\
              implementation "com.quickbirdstudios:opencv:4.5.3.0"\
              implementation "org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.1"' android/app/build.gradle

          echo "✅ Gradle configurations patched successfully."

      # 5. 放置模型
      - name: Place TFLite Model
        run: |
          mkdir -p android/app/src/main/assets
          if [ -f "best_float16.tflite" ]; then
            cp best_float16.tflite android/app/src/main/assets/
          else
            echo "❌ Error: Model not found!"
            touch android/app/src/main/assets/best_float16.tflite
          fi

      - name: Install Dependencies
        run: flutter pub get

      - name: Generate App Icon
        run: |
          if [ -f "assets/images/icon.png" ]; then
             dart run flutter_launcher_icons
          fi

      - name: Build APK
        run: flutter build apk --release --target-platform android-arm64

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: LofterFixer-Release
          path: build/app/outputs/flutter-apk/app-release.apk