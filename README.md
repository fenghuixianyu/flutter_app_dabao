# 📹 视频工坊 (Video Cutter)

无损视频剪切与智能拼接工具

## ✨ 功能特点

- **智能剪切**：输入 `1-2` 即可剪切1到2分钟的片段
- **多段合并**：支持多个时间片段剪切后自动合并
- **无损拼接**：同规格视频秒级无损拼接
- **智能转码**：不同规格视频自动适配重编码
- **覆盖模式**：可选择替换原文件

## 🚀 云打包使用

1. 将此项目上传至 GitHub 仓库
2. 在仓库的 **Actions** 页面
3. 选择 **Flutter Build APK** 工作流
4. 点击 **Run workflow**
5. 等待构建完成，下载 APK 安装包

## 📂 项目结构

```
video_cutter/
├── lib/
│   ├── main.dart           # 入口文件
│   ├── screens/            # 页面
│   │   ├── smart_cut_screen.dart    # 智能剪切
│   │   ├── auto_stitch_screen.dart  # 无损拼接
│   │   └── settings_screen.dart     # 设置
│   ├── services/           # 服务层
│   │   └── ffmpeg_service.dart      # FFmpeg 封装
│   └── utils/              # 工具类
│       └── time_parser.dart         # 时间解析
├── assets/images/          # 图片资源
├── .github/workflows/      # CI/CD 配置
│   └── build.yml           # 云打包脚本
└── pubspec.yaml            # 项目配置
```

## ⚙️ 自定义

### 修改 APP 名称

编辑 `.github/workflows/build.yml`，修改顶部的 `env` 变量：

```yaml
env:
  APP_NAME: "你的APP名称"
  FILE_NAME: "YourAppName"
```

### 修改 APP 图标

将图标文件放入 `assets/images/icon.png`（推荐 1024x1024 像素）

## 📝 时间格式说明

| 输入 | 含义 |
|------|------|
| `1-2` | 1分钟 到 2分钟 |
| `1.5-3` | 1分30秒 到 3分钟 |
| `0:30-1:45` | 30秒 到 1分45秒 |
| `00:01:00-00:02:30` | 1分钟 到 2分30秒 |

## 🔧 技术栈

- Flutter 3.19.0+
- ffmpeg_kit_flutter_min_gpl
- Material Design 3

## 📄 许可证

FFmpeg 相关代码遵循 GPL 协议
